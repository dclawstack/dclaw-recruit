{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dclaw-recruit.serviceAccountName" . }}
  labels:
    {{- include "dclaw-recruit.labels" . | nindent 4 }}
{{- end }}
