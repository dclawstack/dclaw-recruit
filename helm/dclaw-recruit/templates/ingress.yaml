{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-recruit.fullname" . }}
  labels:
    {{- include "dclaw-recruit.labels" . | nindent 4 }}
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-recruit.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
{{- end }}
